# ============================================================
#  매뉴얼 음성 도우미 — Python 의존성
#  설치:  pip install -r requirements.txt
# ============================================================

# ── 백엔드 (API 서버) ──
fastapi
uvicorn[standard]
pydantic
pydantic-settings
python-dotenv
python-multipart           # 음성 파일 업로드

# ── 문서 처리 (PDF 렌더 · OCR · 청킹) ──
# 주의: 수집한 매뉴얼이 레거시 한국어 인코딩(KSCpc-EUC 등)이라 텍스트 레이어가
#       깨져서 추출 불가. → 페이지를 이미지로 렌더 후 한국어 OCR로 텍스트를 얻는다.
pymupdf                    # PDF 페이지 렌더링 (fitz)
pytesseract                # 로컬 Tesseract OCR 래퍼 (torch 불필요) — Tesseract 바이너리 별도 설치 필요
easyocr                    # (대안) 한국어 OCR — torch/CUDA 사용
pillow                     # 이미지 처리 (OCR 입력)
pypdf                      # 보조: 텍스트 레이어가 정상인 PDF용
tiktoken                   # 토큰 단위 청킹

# ── 임베딩 / Reranker ──
sentence-transformers
FlagEmbedding              # BGE-M3 임베딩 + reranker
torch                      # ※ CUDA 빌드는 cu121 인덱스에서 따로 설치 (아래 README/지시 참고)
torchvision                # easyocr 의존 — torch와 같은 cu121 빌드로 맞출 것

# ── 검색 ──
rank-bm25                  # BM25 키워드 검색
chromadb                   # 벡터 DB (기본)
faiss-cpu                  # 대안 벡터 인덱스 (faiss-gpu 가능)

# ── LLM (답변 생성 / 평가셋 생성) ── 기본: Gemini ──
google-genai               # Gemini SDK (from google import genai)
openai                     # 대안 제공자
anthropic                  # 대안 제공자

# ── STT / TTS ──
faster-whisper             # 로컬 GPU Whisper
requests                   # CLOVA Speech/Voice REST 호출

# ── 평가 / 실패 클러스터링 ──
numpy
pandas
scikit-learn               # 클러스터링 (KMeans 등)
matplotlib
seaborn

# ── 유틸 ──
tqdm
loguru

# ── 개발 / 노트북 ──
jupyter
pytest
ruff
